#ifndef EXPRESSION_HPP
#define EXPRESSION_HPP

using namespace std;

class Expression
{
    private:
        string input;
    public:
        Expression(string userInput) {
            input = userInput;
        };
};

#endif
